# 기여 안내

**기여를 받고 싶은 곳은 [`data/`](data/) 입니다.** 직업별 전투 수치와 룬 조건부 옵션이
JSON 으로 들어 있고, 대부분 한 사람이 게임 안에서 확인해 채운 것이라 틀린 곳이 많습니다.
자기 직업을 잘 아는 분이 고쳐주시면 그게 이 프로젝트에 가장 필요한 기여입니다.

**[HELP-WANTED.md](HELP-WANTED.md) 에 검증이 필요한 항목이 정리돼 있습니다.** 자동 생성되는 목록이라 항상 최신입니다.

운영 중인 사이트의 배포·인프라는 이 저장소 밖에서 관리합니다. `data/` 를 고치고 검증하는
데는 필요하지 않습니다.

---

## 어디로 보내면 되나

| | 어디로 |
| --- | --- |
| **수치가 다르다** (개발 안 하심) | [rune.askhyung.com/report](https://rune.askhyung.com/report) — 세 칸이면 끝, GitHub 계정 불필요 |
| **`data/` 를 직접 고치겠다** | 이 저장소에 PR |
| **버그·계산 오류·코드 제안** | [이슈](../../issues/new) |

수치 제보를 이슈로 받지 않는 이유는 간단합니다. 게임을 잘 아는 분 대다수는 GitHub 계정이
없고, 있어도 이슈를 열지 않습니다. 그래서 사이트 안에 창구를 따로 뒀습니다.

어느 쪽이든 **어떻게 확인하셨는지**를 같이 적어주세요. "80%가 맞아요" 만 있으면 반영할 수
없습니다. 근거가 있어야 다른 사람이 검증하거나 반박할 수 있고, 나중에 값이 바뀌었을 때
왜 그랬는지 남습니다. 게임 스크린샷, 스톱워치 측정, 툴팁 사진 전부 좋은 근거입니다.

---

## 직접 고치실 분 — 데이터만 고치면 됩니다

직업 수치는 전부 `data/jobs/<직업>.json` 에 있습니다. JS 를 몰라도 숫자와 문장만 고치면 됩니다.

의존성 설치(`npm install`)는 필요 없습니다. Node.js 20 이상만 있으면 됩니다.

```bash
git clone https://github.com/mobinogi-tools/rune-optimizer.git
cd rune-optimizer
npm run dev     # http://127.0.0.1:8123 — 켜두고 작업합니다
```

그다음은 이 다섯 단계를 돕니다.

1. **`data/jobs/검술사.json` 을 고칩니다** — 숫자와 문장만 바꾸면 됩니다
2. **`node tools/build-data.mjs`** — 고친 데이터를 앱이 읽는 형태(`src/gen/`)로 다시 만듭니다
3. **`npm test`** — 검증기와 계산 테스트가 함께 돕니다
4. **브라우저를 새로고침** — 바꾼 값이 화면에서 어떻게 나오는지 봅니다
5. **`src/gen/` 까지 함께 커밋하고 PR** 을 엽니다

2번을 빠뜨리면 데이터는 고쳐졌는데 앱은 옛 값으로 도는 상태가 됩니다. 테스트가 잡지만,
**생성물까지 커밋**해야 한다는 것을 기억해주세요.

데이터 형식만 빨리 보고 싶으면 `node tools/validate-data.mjs` 만 따로 돌려도 됩니다.
어느 파일 어느 항목이 왜 틀렸는지 한국어로 짚어줍니다.

### `npm run impact` — 어떤 룬이 움직였는지 봅니다

```bash
npm run impact          # 마지막 커밋 대비 지금 작업본
npm run impact main     # 다른 기준과 비교
```

달라진 룬만 늘어놓습니다 — 점수, 「계산 밖」 항목, 부정 효과, 계열, 쿨감 환산, 조건부까지.
통과·실패가 없으므로 **눈으로 읽어야 합니다.** 고친 것 셋을 예상했는데 다섯이 나오면
나머지 둘이 이 도구의 용건입니다. PR 설명에 이 출력을 붙여주시면 리뷰가 훨씬 빠릅니다.

`npm test` 가 이걸 대신하지 못합니다. 점수 테스트는 룬 여섯 개짜리 세트 하나를 고정하므로
그 세트 밖의 룬은 통째로 바뀌어도 초록불입니다.

### 테스트가 빨갛게 나올 때

**골든 점수 테스트는 수치를 고치면 정당하게 깨집니다.** 고정된 룬 세트의 점수를 못박아
두는 테스트라, 데이터가 바뀌면 점수도 바뀌는 게 맞습니다.

그때는 `tests/golden-score.test.mjs` 의 숫자를 새 값으로 갱신하고, **PR 에 왜 바뀌었는지
적어주세요.** 그 갱신이 diff 에 남아서 "이 수정이 결과를 몇 % 움직였는지" 가 눈에 보이게
됩니다 — 실패 메시지가 그 퍼센트를 알려줍니다.

반대로 **`data/` 를 안 고쳤는데 이게 깨졌다면 뭔가 잘못된 것입니다.** 숫자만 맞춰서
통과시키지 마시고 무엇이 계산을 건드렸는지 찾아주세요.

### 수치를 바꿀 때는 evidence 를 같이 고쳐주세요

```json
"effects": { "critical.runeCriticalRatePercent": 40 },
"confidence": "high",
"evidence": [
  { "type": "measured", "date": "2026-08-20",
    "note": "3분 로테이션 3회 반복, 집중 끊긴 시간 평균 35초 → 가동률 80%" }
]
```

**게임에 적혀 있는 숫자면 두 칸이면 끝입니다.** 설명을 짜낼 필요 없습니다.

```json
"evidence": [ { "type": "tooltip", "date": "2026-08-05" } ]
```

- `type` — `measured`(직접 재봄) / `tooltip`(게임 내 툴팁) / `official`(공식 공지) / `community`(커뮤니티 제보)
- **`tooltip` · `official` 은 `type` 과 `date` 만 있으면 됩니다.**
- **`measured` 는 `note` 가 필수입니다.** 툴팁에 없는 값(가동률, 기대 중첩 등)이 여기 해당하고,
  어떻게 뽑았는지가 없으면 아무도 검증도 반박도 못 합니다. "실측했음" 만으로는 안 됩니다.
- **`community` 도 `note` 가 필수입니다.** 유저 제보는 무엇이라고 하는지가 없으면 확인할
  길이 없습니다.
- `date` 는 어느 타입이든 받습니다. **게임이 패치되기 때문입니다** — 날짜가 있어야 나중에
  값이 어긋났을 때 "게임이 바뀐 건가, 처음부터 틀렸던 건가" 를 가릅니다.
- `confidence` — `high`(툴팁 수치가 명확) / `medium`(해석이 들어감) / `low`(간접 추정)

정리하면 **근거의 무게를 검증 비용에 맞춥니다.** 게임 켜고 5초면 확인되는 값에는 짧게,
사람마다 답이 갈리는 값에는 길게. 근거 없는 값이 한 번 들어가면 나중에 아무도 그게 맞는지
틀린지 알 수 없게 되는데, 막으려는 건 그것뿐입니다.

### 직접 피해(직피)는 effects 로 넣지 마세요

"5초마다 공격력의 101%", "빛의 화살 6발이 각각 25%" 처럼 **타격이 따로 나가는** 효과입니다.
피증·치명타 같은 배율이 아니라 별도 공격이라, *한 대 대미지* 공식에 더할 자리가 없습니다.

수치가 커 보여서 넣고 싶어지는데, 넣으면 **그 룬만 부당하게 유리해집니다.** 같은 종류의
직접 피해(황금 아티팩트, 심판 폭발 등)는 전부 계산 밖이라 기준이 어긋나기 때문입니다.

데이터에서는 `field` 대신 `uncounted` 로 적습니다 — "값이 빠졌다" 와 "계산할 자리가 없다"
를 구분하기 위해서입니다. 검증기가 둘을 함께 쓰는 것을 막습니다.

```json
{ "id": "erosion-direct", "label": "직접 피해(침식 분기)", "branch": "erosion",
  "uncounted": "5초마다 (공격력×101%) 의 직접 피해. 타격이 따로 나가는 것이라 자리가 없다",
  "basis": "derived", "note": "...", "evidence": [ ... ] }
```

**왜 빼도 괜찮은가:** 직접 피해는 `공격력 × %` 라서 피증·강화·치명타 배율을 타지 않습니다.
스펙이 오를수록 일반 타격만 커지고 직피 몫은 상대적으로 줄어듭니다. 즉 이 누락은 스펙이
낮을수록 크고 높을수록 작아지며, 방향은 **해당 룬을 과소평가**하는 쪽입니다.

고칠 파일이 어디인지 막히면 [docs/PLACEMENT.md](docs/PLACEMENT.md) 를 보세요.

### 한계는 어느 자리에 적는가 — 두 곳뿐입니다

같은 사실을 두 군데 적으면 그때부터 둘 중 하나는 반드시 낡습니다. 자리가 정해져 있습니다.

| 무엇 | 어디 | 예 |
|---|---|---|
| **항목 하나**가 계산에서 빠진다 | 그 데이터 옆의 `uncounted` | 무형의 침식 분기, 황금 아티팩트 |
| **도구 전체**에 걸린다 | `data/limits.json` | DPS 가 아님, 마도저항 제외, 초월 미구현 |

둘 다 화면과 문서가 **읽어서 그립니다**. `README.md`·`LIMITS.md`·`limits.html` 에 문장을
직접 쓰지 마세요 — 뒤의 둘은 생성물이고, README 는 링크만 갖습니다.

`data/limits.json` 은 `why`(왜 그런가)와 `effect`(그래서 결과가 어떻게 되는가)를 둘 다
요구합니다. 한 줄로만 적으면 "안 한다"로 읽히고, 그 다음에 오는 것은 논쟁뿐이라서입니다.
`kind` 가 `structural` 이면 밖에서 채울 수 없다는 뜻이므로 `openTo` 를 달 수 없습니다.

### 발동률(연타율·강타율·멀티히트율)은 합이 100%를 넘어도 정상입니다

한 타격이 연타이면서 동시에 강타일 수 있어서, 셋은 서로 독립입니다. 실제 세팅에서
연타 99% + 강타 88% 처럼 합이 187%가 나오는 것은 오류가 아닙니다.

**합을 100%로 정규화하거나 서로 배타로 만드는 PR 은 받지 않습니다.** 얼핏 "확률의 합이
1을 넘는다"는 버그로 보이지만, 그렇게 고치면 강화 수치의 기여가 줄어들어 모든 추천이
조용히 바뀝니다. `tests/calculator.test.mjs` 가 이 성질을 막고 있습니다.

### effects 경로는 정해진 것만 씁니다

`data/effect-fields.json` 에 있는 경로만 계산에 반영됩니다. 여기 없는 경로를 쓰면
**에러 없이 조용히 무시**되기 때문에, 검증기가 미리 막습니다.

새 자리가 필요한 종류의 효과(예: "궁극기 비중만큼 반영돼야 한다", "도트 피해 증가")는
데이터만으로 안 되고 계산식 자체를 고쳐야 합니다. **PR 말고 이슈로 먼저 제안해주세요.**

### 룬 조건부 수치를 고치실 분

전투 중에만 붙는 룬 효과(중첩, 발동 조건 등)는 `data/rune-conditionals.json` 의
`RUNE_CONDITIONALS` 에 있습니다. 직업 데이터와 형식은 비슷하지만 항목마다 필드가 몇 개 더 있습니다.

```json
"거대한 분노": [
  {
    "id": "skill-damage",
    "field": "damageIncrease.skillDamagePercent",
    "label": "스킬 피해%",
    "min": 0, "expected": null, "max": 12,
    "expectedFrom": "streak",
    "perStack": 3, "maxStacks": 4, "streakRate": "heavyRatePercent",
    "basis": "derived",
    "note": "강타 적중마다 +3%, 최대 4중첩. 강타가 아닌 공격이 들어오면 즉시 0.",
    "evidence": [
      { "type": "tooltip", "date": "2026-08-05" }
    ]
  }
]
```

- `id` — 사용자 조정값의 저장 키입니다. **한 번 배포되면 사실상 못 바꿉니다** — 바꾸면 그
  옵션에 대한 기존 사용자의 조정값이 조회에서 빗나가 조용히 기본값으로 돌아갑니다. 소문자
  영문 슬러그만 씁니다(예: `heavy-damage`). 라벨을 그대로 붙이면 안 됩니다 — 그러면 문구를
  다듬는 것만으로 조정값이 날아갑니다.
- `label` — 화면에 뜨는 문구입니다. 표시 전용이라 자유롭게 다듬어도 됩니다.
- `field` — 위와 같은 화이트리스트 규칙입니다. `data/effect-fields.json` 에 있는 경로만.
- `expectedFrom` — 기대값을 계산식으로 낼 때만 씁니다. `build-evaluator.mjs` 의
  `EXPECTED_FROM_NAMES` 에 있는 이름만 허용됩니다(`streak`, `stacks`, `erosion` 등). 새
  계산식이 필요하면 데이터만으로 안 됩니다 — 이슈로 먼저 제안해주세요.
- `min` / `expected` / `max` — 슬라이더 범위와 기본 기대값입니다. `expectedFrom` 이 있으면
  `expected` 는 `null` 로 둡니다(계산으로 채워집니다).
- `basis` — `derived`(수치를 계산으로 뽑음) / `playstyle`(플레이 방식에 따라 달라져 사람이
  가정함).
- `evidence` — 직업 데이터와 같은 규칙입니다. 룬 설명 텍스트에서 계산한 값이면
  `type: "tooltip"`, 직접 재본 값이면 `type: "measured"`(`note` 필수)를 씁니다.

---

## 화면을 고치실 분

`runes.html` 과 `runes.css` 는 자유롭게 고치셔도 됩니다.

`src/` 아래는 계산 코드입니다. 화면 작업 중에 이쪽을 고치게 됐다면 PR 설명에 이유를 적어주세요.
`npm test` 의 점수 테스트가 깨진다면 계산 결과가 바뀐 것이므로 의도한 변경인지 확인이 필요합니다.

---

## AI 코딩 도구를 쓰신다면

써도 됩니다. 다만 이 프로젝트에서 가장 다루기 어려운 PR 이 **AI 가 만든, 문법은 완벽하고 설명은
그럴듯한데 게임 사실과 다른 변경**입니다. 사람이 손으로 짠 실수는 대개 눈에 띄지만 이건 안 띕니다.

그래서 다음을 지켜주세요.

1. **PR 하나에 한 가지만.** 수치 하나를 고치러 왔는데 200줄이 바뀌어 있으면 리뷰할 수 없어 닫습니다.
   AI 가 "겸사겸사" 리팩터링한 부분은 빼주세요.
2. **`data/` 만 고치는 PR 과 `src/` 를 고치는 PR 을 분리해주세요.** 데이터 수정에 코드 변경이
   섞이면 어느 쪽이 결과를 바꿨는지 알 수 없습니다.
3. **evidence 는 직접 써주세요.** AI 에게 근거를 지어내게 하지 마세요. 그럴듯한 가짜 근거는
   틀린 수치보다 나쁩니다 — 틀린 수치는 언젠가 발견되지만, 가짜 근거는 검증된 것처럼 보입니다.
4. **자기가 아는 것만 보내주세요.** 자기 직업이 아니면 이슈로 제보만 해주셔도 충분합니다.

리뷰할 때 저희가 확인하는 것은 "코드가 돌아가는가" 가 아니라 **"이 사람이 이걸 실제로 아는가"** 입니다.
`npm test` 는 형식만 봅니다. 게임 사실은 사람이 봅니다.

---

## 데이터 출처

게임에서 확인하신 값이면 `{ "type": "tooltip", "date": "..." }` 두 칸이면 끝입니다.
확인이 안 된 값은 `confidence` 를 낮추고 `note` 에 무엇까지 확인했는지 적어주세요.

대미지 공식은 [디시인사이드 에반 갤러리의 공식 정리글](https://gall.dcinside.com/mgallery/board/view/?id=enban&no=134)
을 따랐습니다. 커뮤니티 추정 공식이며 게임이 확인해준 것이 아닙니다.

**마도저항은 일부러 빼두었습니다.** L항에 들어가고 미달 시 1000마다 절반이라 절대 대미지에는
큰 항이지만, 이걸 주는 룬이 하나도 없어서 모든 후보에 똑같이 곱해집니다 — 순위가 안 바뀝니다.
식은 `src/calculator.mjs` 의 L항 주석에 적어두었으니, **마도저항을 주는 룬이 생기면** 그때는
상수가 아니게 되므로 반드시 넣어야 합니다.

**현재 맞춰둔 기준: `26.07.19 업뎃` 판.** 이 글은 계속 고쳐지고 상수도 바뀝니다(방어 항의 나눗수가
10328 → 14828 로 바뀐 적이 있습니다). 원문이 갱신됐다면 `src/calculator.mjs` 를 대조하고,
`runes.html` 헤더의 날짜 표기도 **같이** 고쳐주세요 — 한쪽만 고치면 화면이 거짓말을 합니다.
